info:
  name: Encrypt
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/post
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
