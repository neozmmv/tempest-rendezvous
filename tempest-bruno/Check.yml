info:
  name: Check
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/post
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "message": "oi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
